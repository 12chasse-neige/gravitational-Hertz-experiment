# Scientific stack (used across metricCalculate, fourier, bestPosition, quantumNoise, noiseAnalysis)
numpy>=1.24,<3
matplotlib>=3.7,<4
scipy>=1.10,<2
PyYAML>=6,<7

# Gravitational-wave interferometer noise budgets (gwinc, Struct, etc.)
gwinc>=0.6,<0.7

# The remaining imports are from the Python standard library or project modules.
